package model.ladder;

import java.util.List;

public class Lines {
    // Line의 일급 컬렉션
    private final List<Line> lines;
    // 라인을 만든다. 라인을 체크한다.

    public Lines(List<Line> lines) {
        this.lines = lines;
    }

    // 라인을 검사한다.

    // 라인을 생성한다.
}
